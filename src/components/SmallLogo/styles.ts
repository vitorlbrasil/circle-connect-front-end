import styled from "styled-components";

export const SmallLogoContainer = styled.header`
  display: flex;
  justify-content: center;
  background-color: var(--primary-color);
  width: 100%;
  padding: 0.5rem;

  h1 {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--grey-0);
  }
`;
